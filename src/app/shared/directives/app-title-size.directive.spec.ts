import { AppTitleSizeDirective } from './directives/app-title-size.directive';

describe('AppTitleSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTitleSizeDirective();
    expect(directive).toBeTruthy();
  });
});
